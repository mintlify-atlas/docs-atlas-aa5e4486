---
title: 'Units'
description: 'Manage academic units organized by mathematical areas within sections'
---

## Overview

Units represent academic units of study within a section. Each unit belongs to a specific section and mathematical area (Aritmética, Álgebra, Geometría, or Trigonometría). Units can contain multiple topics and are organized by a customizable order.

## Endpoints

### List All Units

<CodeGroup>
```bash cURL
curl -X GET "https://api.example.com/api/units" \
  -H "Accept: application/json"
```

```javascript JavaScript
fetch('https://api.example.com/api/units', {
  method: 'GET',
  headers: {
    'Accept': 'application/json'
  }
})
.then(response => response.json())
.then(data => console.log(data));
```

```php PHP
$client = new \GuzzleHttp\Client();
$response = $client->get('https://api.example.com/api/units', [
  'headers' => [
    'Accept' => 'application/json'
  ]
]);
$data = json_decode($response->getBody());
```
</CodeGroup>

#### Response

<ResponseField name="success" type="boolean">
  Indicates if the request was successful
</ResponseField>

<ResponseField name="message" type="string">
  Response message: "Unidades listadas con éxito"
</ResponseField>

<ResponseField name="data" type="array">
  Array of unit objects
  
  <Expandable title="Unit Object">
    <ResponseField name="id" type="integer">
      Unique identifier for the unit
    </ResponseField>
    
    <ResponseField name="section_id" type="integer">
      Foreign key reference to the parent section
    </ResponseField>
    
    <ResponseField name="area" type="enum">
      Mathematical area: "Aritmética", "Álgebra", "Geometría", or "Trigonometría"
    </ResponseField>
    
    <ResponseField name="unidad" type="string">
      Name of the unit
    </ResponseField>
    
    <ResponseField name="descripcion" type="text" optional>
      Description of the unit content (nullable)
    </ResponseField>
    
    <ResponseField name="orden" type="integer">
      Display order of the unit (default: 0)
    </ResponseField>
    
    <ResponseField name="created_at" type="timestamp">
      When the unit was created
    </ResponseField>
    
    <ResponseField name="updated_at" type="timestamp">
      When the unit was last updated
    </ResponseField>
  </Expandable>
</ResponseField>

<CodeGroup>
```json Example Response
{
  "success": true,
  "message": "Unidades listadas con éxito",
  "data": [
    {
      "id": 1,
      "section_id": 1,
      "area": "Aritmética",
      "unidad": "Números Naturales",
      "descripcion": "Introducción a los números naturales y sus propiedades",
      "orden": 1,
      "created_at": "2026-03-05T10:30:00.000000Z",
      "updated_at": "2026-03-05T10:30:00.000000Z"
    },
    {
      "id": 2,
      "section_id": 1,
      "area": "Álgebra",
      "unidad": "Expresiones Algebraicas",
      "descripcion": "Fundamentos de álgebra y manipulación de expresiones",
      "orden": 2,
      "created_at": "2026-03-05T10:31:00.000000Z",
      "updated_at": "2026-03-05T10:31:00.000000Z"
    }
  ]
}
```
</CodeGroup>

---

### Create a Unit

<CodeGroup>
```bash cURL
curl -X POST "https://api.example.com/api/units" \
  -H "Accept: application/json" \
  -H "Content-Type: application/json" \
  -d '{
    "section_id": 1,
    "area": "Geometría",
    "unidad": "Figuras Planas",
    "descripcion": "Estudio de polígonos y figuras geométricas en el plano",
    "orden": 3
  }'
```

```javascript JavaScript
fetch('https://api.example.com/api/units', {
  method: 'POST',
  headers: {
    'Accept': 'application/json',
    'Content-Type': 'application/json'
  },
  body: JSON.stringify({
    section_id: 1,
    area: 'Geometría',
    unidad: 'Figuras Planas',
    descripcion: 'Estudio de polígonos y figuras geométricas en el plano',
    orden: 3
  })
})
.then(response => response.json())
.then(data => console.log(data));
```

```php PHP
$client = new \GuzzleHttp\Client();
$response = $client->post('https://api.example.com/api/units', [
  'headers' => [
    'Accept' => 'application/json',
    'Content-Type' => 'application/json'
  ],
  'json' => [
    'section_id' => 1,
    'area' => 'Geometría',
    'unidad' => 'Figuras Planas',
    'descripcion' => 'Estudio de polígonos y figuras geométricas en el plano',
    'orden' => 3
  ]
]);
$data = json_decode($response->getBody());
```
</CodeGroup>

#### Request Body

<ParamField name="section_id" type="integer" required>
  ID of the section this unit belongs to. Must reference a valid section.
</ParamField>

<ParamField name="area" type="enum" required>
  Mathematical area. Must be one of:
  - "Aritmética"
  - "Álgebra"
  - "Geometría"
  - "Trigonometría"
</ParamField>

<ParamField name="unidad" type="string" required>
  Name of the unit
</ParamField>

<ParamField name="descripcion" type="text" optional>
  Description of the unit content. This field is nullable.
</ParamField>

<ParamField name="orden" type="integer" default="0">
  Display order of the unit. Defaults to 0 if not provided.
</ParamField>

#### Response

<ResponseField name="success" type="boolean">
  Indicates if the request was successful
</ResponseField>

<ResponseField name="message" type="string">
  Response message: "Unidad creada con éxito"
</ResponseField>

<ResponseField name="data" type="object">
  The created unit object
  
  <Expandable title="Unit Object">
    <ResponseField name="id" type="integer">
      Unique identifier for the unit
    </ResponseField>
    
    <ResponseField name="section_id" type="integer">
      Foreign key reference to the parent section
    </ResponseField>
    
    <ResponseField name="area" type="enum">
      Mathematical area
    </ResponseField>
    
    <ResponseField name="unidad" type="string">
      Name of the unit
    </ResponseField>
    
    <ResponseField name="descripcion" type="text">
      Description of the unit content
    </ResponseField>
    
    <ResponseField name="orden" type="integer">
      Display order of the unit
    </ResponseField>
    
    <ResponseField name="created_at" type="timestamp">
      When the unit was created
    </ResponseField>
    
    <ResponseField name="updated_at" type="timestamp">
      When the unit was last updated
    </ResponseField>
  </Expandable>
</ResponseField>

<CodeGroup>
```json Example Response (201 Created)
{
  "success": true,
  "message": "Unidad creada con éxito",
  "data": {
    "id": 3,
    "section_id": 1,
    "area": "Geometría",
    "unidad": "Figuras Planas",
    "descripcion": "Estudio de polígonos y figuras geométricas en el plano",
    "orden": 3,
    "created_at": "2026-03-05T10:35:00.000000Z",
    "updated_at": "2026-03-05T10:35:00.000000Z"
  }
}
```
</CodeGroup>

---

### Get a Unit

<CodeGroup>
```bash cURL
curl -X GET "https://api.example.com/api/units/1" \
  -H "Accept: application/json"
```

```javascript JavaScript
fetch('https://api.example.com/api/units/1', {
  method: 'GET',
  headers: {
    'Accept': 'application/json'
  }
})
.then(response => response.json())
.then(data => console.log(data));
```

```php PHP
$client = new \GuzzleHttp\Client();
$response = $client->get('https://api.example.com/api/units/1', [
  'headers' => [
    'Accept' => 'application/json'
  ]
]);
$data = json_decode($response->getBody());
```
</CodeGroup>

#### Path Parameters

<ParamField name="id" type="integer" required>
  The ID of the unit to retrieve
</ParamField>

#### Response

<ResponseField name="success" type="boolean">
  Indicates if the request was successful
</ResponseField>

<ResponseField name="message" type="string">
  Response message: "Unidad obtenida con éxito"
</ResponseField>

<ResponseField name="data" type="object">
  The unit object
  
  <Expandable title="Unit Object">
    <ResponseField name="id" type="integer">
      Unique identifier for the unit
    </ResponseField>
    
    <ResponseField name="section_id" type="integer">
      Foreign key reference to the parent section
    </ResponseField>
    
    <ResponseField name="area" type="enum">
      Mathematical area
    </ResponseField>
    
    <ResponseField name="unidad" type="string">
      Name of the unit
    </ResponseField>
    
    <ResponseField name="descripcion" type="text">
      Description of the unit content
    </ResponseField>
    
    <ResponseField name="orden" type="integer">
      Display order of the unit
    </ResponseField>
    
    <ResponseField name="created_at" type="timestamp">
      When the unit was created
    </ResponseField>
    
    <ResponseField name="updated_at" type="timestamp">
      When the unit was last updated
    </ResponseField>
  </Expandable>
</ResponseField>

<CodeGroup>
```json Example Response
{
  "success": true,
  "message": "Unidad obtenida con éxito",
  "data": {
    "id": 1,
    "section_id": 1,
    "area": "Aritmética",
    "unidad": "Números Naturales",
    "descripcion": "Introducción a los números naturales y sus propiedades",
    "orden": 1,
    "created_at": "2026-03-05T10:30:00.000000Z",
    "updated_at": "2026-03-05T10:30:00.000000Z"
  }
}
```
</CodeGroup>

---

### Update a Unit

<CodeGroup>
```bash cURL
curl -X PUT "https://api.example.com/api/units/1" \
  -H "Accept: application/json" \
  -H "Content-Type: application/json" \
  -d '{
    "section_id": 1,
    "area": "Aritmética",
    "unidad": "Números Naturales y Operaciones",
    "descripcion": "Introducción a los números naturales, sus propiedades y operaciones básicas",
    "orden": 1
  }'
```

```javascript JavaScript
fetch('https://api.example.com/api/units/1', {
  method: 'PUT',
  headers: {
    'Accept': 'application/json',
    'Content-Type': 'application/json'
  },
  body: JSON.stringify({
    section_id: 1,
    area: 'Aritmética',
    unidad: 'Números Naturales y Operaciones',
    descripcion: 'Introducción a los números naturales, sus propiedades y operaciones básicas',
    orden: 1
  })
})
.then(response => response.json())
.then(data => console.log(data));
```

```php PHP
$client = new \GuzzleHttp\Client();
$response = $client->put('https://api.example.com/api/units/1', [
  'headers' => [
    'Accept' => 'application/json',
    'Content-Type' => 'application/json'
  ],
  'json' => [
    'section_id' => 1,
    'area' => 'Aritmética',
    'unidad' => 'Números Naturales y Operaciones',
    'descripcion' => 'Introducción a los números naturales, sus propiedades y operaciones básicas',
    'orden' => 1
  ]
]);
$data = json_decode($response->getBody());
```
</CodeGroup>

#### Path Parameters

<ParamField name="id" type="integer" required>
  The ID of the unit to update
</ParamField>

#### Request Body

<ParamField name="section_id" type="integer" required>
  ID of the section this unit belongs to
</ParamField>

<ParamField name="area" type="enum" required>
  Mathematical area: "Aritmética", "Álgebra", "Geometría", or "Trigonometría"
</ParamField>

<ParamField name="unidad" type="string" required>
  Name of the unit
</ParamField>

<ParamField name="descripcion" type="text" optional>
  Description of the unit content
</ParamField>

<ParamField name="orden" type="integer" default="0">
  Display order of the unit
</ParamField>

#### Response

<ResponseField name="success" type="boolean">
  Indicates if the request was successful
</ResponseField>

<ResponseField name="message" type="string">
  Response message: "Unidad actualizada con éxito"
</ResponseField>

<ResponseField name="data" type="object">
  The updated unit object
  
  <Expandable title="Unit Object">
    <ResponseField name="id" type="integer">
      Unique identifier for the unit
    </ResponseField>
    
    <ResponseField name="section_id" type="integer">
      Foreign key reference to the parent section
    </ResponseField>
    
    <ResponseField name="area" type="enum">
      Mathematical area
    </ResponseField>
    
    <ResponseField name="unidad" type="string">
      Name of the unit
    </ResponseField>
    
    <ResponseField name="descripcion" type="text">
      Description of the unit content
    </ResponseField>
    
    <ResponseField name="orden" type="integer">
      Display order of the unit
    </ResponseField>
    
    <ResponseField name="created_at" type="timestamp">
      When the unit was created
    </ResponseField>
    
    <ResponseField name="updated_at" type="timestamp">
      When the unit was last updated
    </ResponseField>
  </Expandable>
</ResponseField>

<CodeGroup>
```json Example Response
{
  "success": true,
  "message": "Unidad actualizada con éxito",
  "data": {
    "id": 1,
    "section_id": 1,
    "area": "Aritmética",
    "unidad": "Números Naturales y Operaciones",
    "descripcion": "Introducción a los números naturales, sus propiedades y operaciones básicas",
    "orden": 1,
    "created_at": "2026-03-05T10:30:00.000000Z",
    "updated_at": "2026-03-05T11:45:00.000000Z"
  }
}
```
</CodeGroup>

---

### Delete a Unit

<CodeGroup>
```bash cURL
curl -X DELETE "https://api.example.com/api/units/1" \
  -H "Accept: application/json"
```

```javascript JavaScript
fetch('https://api.example.com/api/units/1', {
  method: 'DELETE',
  headers: {
    'Accept': 'application/json'
  }
})
.then(response => response.json())
.then(data => console.log(data));
```

```php PHP
$client = new \GuzzleHttp\Client();
$response = $client->delete('https://api.example.com/api/units/1', [
  'headers' => [
    'Accept' => 'application/json'
  ]
]);
$data = json_decode($response->getBody());
```
</CodeGroup>

#### Path Parameters

<ParamField name="id" type="integer" required>
  The ID of the unit to delete
</ParamField>

#### Response

<ResponseField name="success" type="boolean">
  Indicates if the request was successful
</ResponseField>

<ResponseField name="message" type="string">
  Response message: "Unidad eliminada con éxito"
</ResponseField>

<ResponseField name="data" type="null">
  Returns null for successful deletions
</ResponseField>

<CodeGroup>
```json Example Response
{
  "success": true,
  "message": "Unidad eliminada con éxito",
  "data": null
}
```
</CodeGroup>

---

## Relationships

### Section (Parent)

Each unit must belong to a section. The `section_id` field is a foreign key that references the `sections` table. When a section is deleted, all its units are automatically deleted (cascade delete).

### Topics (Children)

A unit can have many topics. When a unit is deleted, all associated topics are automatically deleted (cascade delete).

## Database Schema

The units table includes:

- `id` - Primary key (auto-increment)
- `section_id` - Foreign key to sections table (cascade delete)
- `area` - Enum field: "Aritmética", "Álgebra", "Geometría", "Trigonometría"
- `unidad` - String field for unit name
- `descripcion` - Text field for description (nullable)
- `orden` - Integer field for display order (default: 0)
- `created_at` - Timestamp for creation
- `updated_at` - Timestamp for last update